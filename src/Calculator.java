i
public class Calculator {

    public static final double OMEGA = 0.5;
    public static final double FIELDS_MODULE = 1;

    public double getDerPsi(double t) {
        if (t < (Math.PI / (2 * OMEGA)))
            return FIELDS_MODULE;
        while (true)

    }

}
